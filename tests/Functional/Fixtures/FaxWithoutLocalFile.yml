App\Entity\Fax:
    fax_without_media_file:
        faxDirection: 'inbound'
        faxId: '<regexify("[0-9a-zA-Z]{34}")>'
    fax_without_local_file_{1..20}:
        faxDirection: 'inbound'
        faxId: '<regexify("[0-9a-zA-Z]{34}")>'
        mediaUrl: 'https://some.Where.Over/TheRainbow'
